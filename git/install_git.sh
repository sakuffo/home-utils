# update the system to the latest version of git
sudo apt update
sudo apt install git

# configure git to with a user name and email address
git config --global user.name "Stephen Akuffo"
git config --global user.email "sa.akuffo@gmail.com"